{
  "name": "vite",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite && netlify-lambda serve",
    "build": "vite build",
    "serve": "vite preview && netlify-lambda serve"
  },
  "devDependencies": {
    "vite": "^3.0.4"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.15"
  }
}
